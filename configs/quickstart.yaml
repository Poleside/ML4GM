run_name: quickstart-rf-loyo
random_seed: 42
data:
  input: data/sample/glacier_sample.csv
  target: dhdt
  glacier_id: rgiid
  year: year
  output_dir: outputs/quickstart
model:
  name: random_forest
  parameters:
    n_estimators: 40
    max_depth: 6
    n_jobs: 1
validation:
  strategy: loyo
  folds: 5
