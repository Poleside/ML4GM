run_name: full-ml4gm-benchmark
random_seed: 42
data:
  input: data/processed/merged_data.csv
  target: dhdt
  glacier_id: rgiid
  year: year
  output_dir: outputs/benchmark
model:
  name: random_forest
  parameters:
    n_estimators: 500
    max_depth: 16
    n_jobs: -1
validation:
  strategy: block
  folds: 5
